import React from "react";

function Header() {
    return (
        <header className="header">
            <h3 className="header-title btn-primary">
                todo app
            </h3>
        </header>
    );
}

export default Header;